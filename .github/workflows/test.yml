name: push test branch

on:
  push:
    branches:
      - test

jobs:
  build:
    runs-on: [ubuntu-latest]
    steps:
      - name: npm install
        run: npm install
      - name: run jest
        uses: stefanoeb/jest-action@1.0.0
